# slurpentropee

```sh
NAME:
   slurpentropee - Slurps entropy

USAGE:
   slurpentropee [global options] command [command options] [arguments...]

VERSION:
   0.0.0

COMMANDS:
GLOBAL OPTIONS:
   --max value, -m value      Max int for generating RNG ints (default: -1)
   --bytes value, -b value    Bytes to read from RNG (default: 32)
   --timeout value, -t value  Valid time units are 'ns', 'us' (or 'µs'), 'ms', 's', 'm', 'h' (default: "5ms")
   --help, -h                 show help
   --version, -v              print the version
```
